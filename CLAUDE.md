# 프로젝트 에이전트 가이드라인

> **핵심 원칙**: 계획 먼저, 최소한으로, 외과적으로.
> 세부 규칙은 `.claude/rules/` 파일을 참조한다.

---

## 언어 정책
- 모든 대화, 커밋 메시지, 코드 주석: **한국어**

## 작업 전 필수 단계
1. **계획 체크리스트** 먼저 제시 (구현 전)
2. 불확실하면 가정을 명시하고 질문
3. 복잡한 방법보다 **단순한 대안** 먼저 제안

## 3-레이어 아키텍처
- **Layer 1 (지시)**: `directives/` — 목표와 도구 정의 SOP
- **Layer 2 (오케스트레이션)**: AI 의사결정, 도구 라우팅, 오류 처리
- **Layer 3 (실행)**: 실제 코드 및 스크립트

## 요구사항 추적
`Require ID → 처리 방법 → 결과` 순서로 문서화

## 파일 생성 규칙
새 파일 생성 시 즉시 `FILE_MANIFEST.md`에 이름과 역할 추가

## 세부 규칙 참조
- 코딩 스타일 → `.claude/rules/coding.md`
- 환경 및 보안 → `.claude/rules/security.md`
- 파일 구조 → `.claude/rules/structure.md`
