# 포토프레임 키오스크용 sway 최소 설정.
# kiosk-start.sh가 `sway --config`로 이 파일을 지정해 실행한다.

exec $HOME/photoframe-businfo/scripts/kiosk-browser.sh

default_border none
default_floating_border none
for_window [app_id=".*"] fullscreen enable

# 터치 입력 시 커서를 즉시 숨기고, 60초(60000ms) 동안 입력이 없어도 숨긴다.
# 마우스를 움직이면 바로 다시 보인다.
seat seat0 hide_cursor 60000
seat seat0 hide_cursor touch enable
